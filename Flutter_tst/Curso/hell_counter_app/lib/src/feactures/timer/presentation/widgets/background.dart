import 'package:flutter/material.dart';
import 'custom_waves.dart';

class Background extends StatelessWidget {
  const Background({super.key});

  @override
  Widget build(BuildContext context) => const CustomWaves();
}
