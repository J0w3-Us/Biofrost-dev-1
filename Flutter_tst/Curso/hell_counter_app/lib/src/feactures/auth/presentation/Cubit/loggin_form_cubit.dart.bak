import 'package:flutter_bloc/flutter_bloc.dart';

part 'login_form_state.dart';

class LogginFormCubit extends Cubit<LoginFormState> {
  LogginFormCubit() : super(LoginFormState());

  void toggleRememberme(bool newValue) {
    emit(state.copyWith(isRememberChecked: newValue));
  }
}
