generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model ingre {
  id       Int    @id @default(autoincrement())
  nombre   String
  cantidad Int
}
